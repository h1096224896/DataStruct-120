{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/build/${fileBasenameNoExtension}", // 替换为你的可执行文件路径
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            // "runInTerminal": true,
            "console": "integratedTerminal",
            "preLaunchTask": "C/C++: clang 生成活动文件"
        }
    ]
}