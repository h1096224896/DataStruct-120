#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// 假设以不带头结点的循环链表表示队列，并且只设一个指针指向队尾结点，但不设头指针。 编程实现队列的入队和出队操作
typedef struct QNode
{
    int data;
    struct QNode *next;
} QNode, * QueuePtr;


int main()
{
    
    return 0;
}