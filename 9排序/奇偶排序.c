#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void oddAndeven(int* arr, int len) {
    for (size_t i = 0; i < len; i++) {
        if (arr[i] % 2) {  // 奇数
            int temp = 
        }
    }

}

int main() {
    int arr[] = { 3,7,2,6,5,4,9 };
    int len = sizeof(arr) / sizeof(arr[0]);

    return 0;
}